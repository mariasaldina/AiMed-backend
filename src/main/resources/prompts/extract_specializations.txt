Тебе даны:

* структурированный текст, содержащий медицинский контекст. В тексте есть раздел "Рекомендуемые специальности", где перечислены медицинские специальности в свободной форме;
* массив специальностей из базы данных в формате объектов: { id, name }, где name — официальное название специальности.

Твоя задача — сопоставить специальности из текста с элементами массива и вернуть список соответствующих id.

ПРАВИЛА СОПОСТАВЛЕНИЯ:

1. Сопоставляй по смыслу, а не только по точному совпадению строк.
2. Учитывай синонимы.
3. Игнорируй несущественные различия в формулировках.
4. Если специальность из текста соответствует нескольким вариантам — выбирай наиболее точный и медицински корректный.
5. Не придумывай новые id и не используй значения вне предоставленного массива.
6. Если совпадений нет — не добавляй ничего.
7. Максимум 5 id в ответе.

ФОРМАТ ОТВЕТА:
Верни ТОЛЬКО JSON-массив id без пояснений, текста и дополнительных структур.

ПРИМЕР ВЫХОДА:
[1, 5, 12]

ДАННЫЕ:


